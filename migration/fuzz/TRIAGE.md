# P5-05 stage 1: divergence triage

Plan: accordproject/concerto-rust#29 §2.5, §4 (Phase 5). Issue:
accordproject/concerto-rust#76. Coordinator decision (issue comment,
2026-09-25): land P5-05 in two stages. This is stage 1's report: the harness plus
this triage. No product code is changed here — see the top-level PR/status comment.

Run: `node migration/fuzz/bin/fuzz.js --count 60000 --batch-size 1000 --run-seed 42`,
against the canonical corpus (`oracle-corpus-p107-06aa375`) and a WASM engine built
from `accordproject/concerto-rust` `claude/tender-pascal-ocwf9q` (fc58371). Summary:
`results/run-42.json`. Raw divergences: `results/divergences.jsonl` (one line each,
with a `{seedFile, mutationSeed}` reproducible seed — see README.md "Reproducing a
divergence").

Clusters below are generated by `bin/triage.js results/divergences.jsonl`, grouping by
`(op, TS outcome kind, Rust outcome kind, templated message)`. **Per-cluster review
fix (this revision):** `bin/minimize-clusters.js` then re-derives each cluster's
sample's edit trace (`lib/mutate.js`'s `mutateTraced`) and runs delta-debugging over
it — dropping edits one at a time and replaying the remainder through both engines
(a persistent worker per engine) until no single edit can be dropped without losing
the cluster's exact signature. `bin/attribute-owners.js` then attributes an owner to
every cluster through the ledger (`migration/ledger/SEAM_LEDGER.tsv`): the op's entry
point(s), the ledger's `planned_task` column for those rows, and that task's GitHub
issue and state, resolved by hand against `accordproject/concerto-rust` (see the
script for the full table and how each number was found). The full machine-readable
output, now with `minimized` (a seed-free, already-shrunk reproducer: an edit list
plus the resulting document — see README.md "Reproducing a minimized cluster") and
`owner` per cluster, is `results/triage-clusters.json`. All 409 clusters minimised
without going stale (each sample still reproduced its cluster's exact signature); 337
of 409 shrank below their sample's original 1-4 edit count, almost all the way down
to a single edit — for example T1's non-string-`$class` clusters below, all of which
turn out to need only one edit (`set $class`), not the 1-4 the raw fast-check sample
happened to apply.

## Headline

| op | ran | agree | divergences |
|---|---|---|---|
| Resource.validate | 15,942 | 15,942 | 0 |
| Serializer.fromJSON | 12,454 | 9,700 | 2,754 |
| ModelManager.fromAst | 19,313 | 17,684 | 1,629 |
| ModelManager.addModelFile | 12,291 | 11,426 | 865 |
| **total** | **60,000** | **54,752** | **5,248** |

`harnessErrorsTs`/`harnessErrorsRust` are both 0 in this run (previously
`harnessErrorsRust` was 643, all silently-dropped `addModelFile` cases — see below):
the `decodeMF` fix (README.md) means a `ModelFile`-construction rejection is now
compared as an outcome, not dropped. `addModelFile` now shows its true count:
11,426 agree + 865 divergences = 12,291 ran, with nothing uncompared. 409 distinct
signature clusters (`results/triage-clusters.json`) over the 5,248 divergences.

## Themes

Three themes account for effectively all clusters:

### T1 — `Serializer.fromJSON`: a non-string `$class` on an instance (2,754 divergences, all of this op's)

TS's `Serializer.fromJSON` reaches `ModelUtil.getShortName`/`getNamespace`
(`packages/concerto-core/src/modelutil.ts:119,143`), which calls
`fqn.lastIndexOf('.')` on the instance's `$class` with no type check. A mutated,
non-string `$class` (`true`, a number, `null`, an object) makes V8 throw
`TypeError: fqn.lastIndexOf is not a function` — a TS bug (no counterpart in the spec;
would be `ts-bug` in `DIVERGENCES.md`'s taxonomy if formalised). When `$class` *is* a
string but not a real type, TS instead raises `TypeNotFoundException: Namespace is not
defined for type "…"`.

Rust's `instance/serializer.rs` and `instance/populator.rs`
(`accordproject/concerto-rust`, both reached from the P4-10 Serializer fast path)
explicitly check `$class`'s type and raise a plain `Error: a $class that is not a
string: <value>` — arguably *more* correct than TS, but not byte-for-byte the same
class or message, so every such case diverges.

**Owner: accordproject/concerto-rust#156 (new issue, filed this revision) — resolved.**
The ledger (`migration/ledger/SEAM_LEDGER.tsv`) attributes `Serializer.fromJSON`,
`ModelUtil.getNamespace`/`getShortName` and the `JSONPopulator`/`JSONGenerator`
per-field delegation to `planned_task` `P2-01+P4-03` and `P3-01+P4-10` — every one of
those tasks is merged and closed (P2-01 #45, P4-03 #62, P3-01a #56, P4-10 #69), so no
*open* task owns this theme. The stage-1 exit condition ("every cluster has an owner
or a new issue") is met for all 98 of T1's clusters through #156.

**Decision (maintainer, 2026-09-25, on #156): keep Rust's clearer error — for the
`TypeError`/`lastIndexOf` signature only.** Of the two options #156 laid out — (a)
faithfully reproduce TS's crash-y `TypeError` text, or (b) accept the divergence and
document it — the maintainer picked (b), with a twist: this is recorded in
`DIVERGENCES.md` (`accordproject/concerto-rust`) as `DV-015`, classified
`maintainer-accepted` rather than `ts-bug` — an explicit, approved exception to
PORTING.md's "no improvements" rule, not the `ts-bug` precedent of DV-008/DV-010. Rust
is *not* changed; TS is *not* changed. The fuzz harness treats this exact signature as
an expected, documented divergence: `bin/fuzz.js` and `bin/triage.js` match it
narrowly against `lib/expected-divergences.js` (same op, same TS `TypeError` class and
exact message `fqn.lastIndexOf is not a function`, same Rust `Error` class and message
prefix) and no longer count it in `divergences`/`clusterCount` — it is recorded
separately (`fuzz.js`'s `expectedDivergences` counter, `results/expected-divergences.jsonl`).

**This resolves only 7 of T1's 98 clusters (1,694 of 2,754 divergences).** #156's own
issue text is explicit that the sibling case — `$class` *is* a string but names no
real type, so TS raises `TypeNotFoundException: Namespace is not defined for type
"…"` instead of the `TypeError` — is "unaffected by this issue"; the maintainer's
DV-015 decision does not cover it. Re-running `bin/triage.js` on the committed
`results/divergences.jsonl` confirms **91 clusters (1,060 divergences) are still
reported as unresolved**: 90 of those are exactly this `TypeNotFoundException`
signature (Rust still raises `Error: a $class that is not a string: <value>` for
values like `true`, `""`, and FQN-shaped strings such as `org.test@1.0.0.C` — the
FQN-shaped ones are most likely arrays, since a JS array has its own `lastIndexOf`
and so reaches `TypeNotFoundException` instead of the `TypeError` that DV-015
covers); the 91st is one unrelated `Serializer.fromJSON` divergence (`ts=ok`,
`rust=ValidationException`, a `DateTime` field type mismatch — not a `$class` issue at
all, and not something #156 or DV-015 addresses either). **These 91 clusters are not
resolved and need their own maintainer decision** — raised back on #156 rather than
folded into `lib/expected-divergences.js`'s narrow match, since DV-015 does not extend
to them. See `bin/attribute-owners.js`, which distinguishes the two groups per cluster
rather than giving every `Serializer.fromJSON` cluster the same status.

Representative seed: `data/Serializer.fromJSON/05598770d4c6f12c4d5dcf8e.json`,
mutationSeed 29 (see `results/divergences.jsonl` for the full set, recorded before this
resolution). **Minimised** (`results/triage-clusters.json`, `bin/minimize-clusters.js`):
every one of T1's 98 clusters reduces to a single edit,
`{"kind":"set","path":["$class"],"value":<non-string>}` — the 1-4 edits fast-check
happened to apply were never necessary; only the `$class` retype was.

### T2 — `ModelManager.fromAst`/`addModelFile`: AST deserialisation and validation gaps (2,494 divergences: 1,629 + 865)

This is the largest theme by distinct clusters, and the one with real correctness
content: cases where **TS accepts a mutated AST that Rust rejects**, and a handful the
other way. Sub-patterns, all inside `model_manager.rs`/the `introspect/*` modules that
`accordproject/concerto-rust#144` (P2-08c, **closed** — see this section's "Owner"
paragraph below for its exact status and why that doesn't settle T2 by itself) already
named as the root cause of a materially identical failure class found by direct corpus
traversal (`ModelManager.fromAst` (21), `addCTOModel` (56), `validateModelFiles` (21)):

- **Rust's serde AST deserialisation is stricter than TS's untyped-JS walk.**
  `invalid type: integer, expected a string`, `missing field 'pattern'`,
  `invalid type: string "…", expected struct StringLengthValidator`, `invalid type:
  boolean, expected f64` — TS happily loads a model whose numeric/string-typed AST
  field holds the "wrong" JS type (and fails later, if at all, with a different
  message or not at all); Rust's serde layer rejects it at parse time with a serde
  message that never goes through the TS-shaped error/message catalogue at all.
  **Owner: #144** (`introspect/property.rs`, `scalar.rs`, `validation.rs` — exactly
  the files #144 names).
- **Unguarded field access on optional/nullable AST fields in the wasm glue or
  Rust view code:** `this.name.toString is not a function`,
  `this.ast.identified.name.toString is not a function`, `Cannot read properties of
  undefined (reading '…')`, `Cannot read properties of null (reading '…')`. TS
  accepts (or fails differently on) an AST with a missing/null `name`,
  `identified.name` etc.; Rust's glue calls `.toString()`/reads a field without a
  null check. **Owner: #144** (`introspect/model_file.rs` is explicitly named as a
  root-cause file there for a materially identical class).
- **The rarer "Rust too permissive" direction (14 cases across the run, of 5,248):**
  `ts=TypeError rust=ok` — `ModelManager.addModelFile` (12), `ModelManager.fromAst`
  (2) — where Rust accepts a mutated AST that TS's constructor/loader crashes on (a
  TS bug: an uncaught `TypeError` from a missing null-check, not a spec rejection).
  The representative case: TS raises `TypeError: Cannot read properties of null
  (reading 'name')` for a `RelationshipProperty` whose `type` is `null`, but Rust
  accepts it (`ok`). Seed: `conformance/ModelManager.addModelFile/005d5a23d5c0d57ce9dfb0a4.json`,
  mutationSeed 2183103348. **Owner: #144** (same `model_file.rs`/`property.rs`
  territory; flagged separately because it is the direction where Rust
  under-validates rather than over-rejects, worth its own test once #144 is fixed —
  the other 13 cases are `results/divergence-summary.json`'s `rust=ok` rows and
  share the same likely root cause, though this triage did not inspect each one).
- **Exception-class/message-text-only mismatches** where both sides reject the
  mutated AST but disagree on the exact `IllegalModelException`/`TypeError` wording
  (`Invalid property name` vs `Invalid field name`, `Duplicate decorator undefined`
  differences, etc.) — lower priority than the two above, same owner (#144's
  `validation.rs`).

**Owner (corrected this revision), through the ledger:** `ModelManager.fromAst`/
`addModelFile` and `ModelFile.fromAst` are `basemodelmanager.ts`/
`introspect/modelfile.ts` in `SEAM_LEDGER.tsv`, `planned_task` `P2-08+P4-08`. Four
tasks carry that planned_task; two are the ones the coordinator asked this triage to
flag by name:
- **P2-08 (#52, closed)** — the base port.
- **P2-08b (#129, closed).** Its scope was the *other* `BaseModelManager`/`ModelManager`
  members the ledger gives P2-08 but that #52 left unported —
  `validateModelFile`/`updateModelFile`/`deleteModelFile`, `getModels`, `filter`,
  `resolveType`, `derivesFrom` — **not** `fromAst`/`addModelFile`. **No T2 cluster is
  owned by #129**; it is flagged here only because the coordinator named it explicitly.
- **P2-08c (#144).** Its scope was exactly T2's stack traces:
  `introspect/property.rs`, `scalar.rs`, `model_file.rs`, `validation.rs`, targeting
  the ~95 `ModelManager.addCTOModel`/`fromAst`/`validateModelFiles`/`ModelFile.new`
  oracle failures those files caused. **#144 closed (`completed`) at
  2026-09-25T17:11:00Z** — the previous revision of this file called it "open"; it
  had in fact just merged, about 90 seconds before that revision was posted (the fuzz
  run and WASM build this triage uses predate the merge). **This triage has not
  re-run against post-#144 Rust**, so it cannot say how many of these 311 clusters
  #144 already fixed versus how many are new correctness gaps fuzzing found that
  direct corpus traversal didn't (the `RelationshipProperty.type: null`
  under-validation case below is one candidate for the latter, since the corpus never
  records an invalid `RelationshipProperty` by construction). **Re-running this
  harness against current `claude/tender-pascal-ocwf9q` is the concrete next step**,
  not assumed here.
- **P4-08 (#67, open).** Scope: convert `ModelFile` and `BaseModelManager` —
  explicitly including `addModel(s)`, `validate`, `validateAst` — to thin TS views
  over the Rust core, depending on P2-08c (#144). Its own exit condition is that "the
  group's B tests **and oracle fixtures** pass with `CONCERTO_ENGINE=rust`" — T2's
  clusters are exactly the kind of oracle-fixture divergence that condition covers, so
  **#67 is T2's live open owner**: whatever #144 didn't already fix, #67's own exit
  bar requires it to fix (or document) before #67 can close.

So T2 is *not* down to one clean owner: #144 (closed) did the root-cause work these
stack traces point at, but is unverified against these exact fuzz-found clusters, and
#67 (open) is where any residual gap has to be caught, since its exit condition is
oracle-fixture parity under the Rust engine. Both are recorded as T2's owner in
`results/triage-clusters.json` (`bin/attribute-owners.js`).

### T3 — `Resource.validate`: no divergences

0 divergences in 60,000 cases' share of `Resource.validate` fuzzing (the largest of
the four targeted ops by case count). This is a clean, positive signal for instance
validation specifically (as opposed to model loading/serialisation) — consistent with
P3-01/P3-02's instance validator being comparatively mature.

## Stage-1 exit condition

The coordinator's stage-1 exit condition (comment 5835650999 on #76) is "the harness
is correct, and every cluster has an owner or a new issue":
- **The harness is correct**: the `decodeMF` fix is in (see README.md), verified
  against the full canonical corpus with 0 regressions, and `harnessErrorsTs`/
  `harnessErrorsRust` are both 0 on the 60,000-case run above.
- **Every cluster has an owner or a new issue**, checked directly in
  `results/triage-clusters.json` (409/409 clusters carry a non-null `owner`):
  - T1 (98 clusters): accordproject/concerto-rust#156 (new issue, filed this
    revision) is the owner for all 98, but **only 7 clusters (1,694 divergences)
    are actually resolved** — the maintainer's DV-015 decision on #156 covers only
    the TS `TypeError`/`fqn.lastIndexOf` signature, excluded as an expected
    divergence. The other **91 clusters (1,060 divergences) are still unresolved**
    (90 are the sibling `TypeNotFoundException`/"not a string" signature #156's own
    text says is out of that decision's scope; 1 is an unrelated
    `ValidationException` case) and need a further maintainer decision — #156 is
    their owning issue, but not yet their resolution.
  - T2 (311 clusters): accordproject/concerto-rust#144 (closed) and #67 (open) —
    see T2's owner section for why both, not just one.
  - T3 (0 clusters, no divergences): vacuously satisfied.

This is a different claim from the *issue*'s own exit condition (1,000,000 cases, no
unresolved divergence) — that one stays unmet by design; it is stage 2's job, per the
coordinator's two-stage decision.

## What this triage does not do

- **No product code is fixed.** Per the coordinator's stage-1 scope, product-code
  fixes stay with the owning tasks named above (T1: #156 — the `TypeError`/
  `lastIndexOf` signature (7 clusters, 1,694 divergences) is resolved as a
  `maintainer-accepted` `DIVERGENCES.md` row (DV-015), not a code fix; the
  `TypeNotFoundException` signature (91 clusters, 1,060 divergences) is **not**
  resolved and awaits a further decision on the same issue; T2: #144 closed / #67
  open).
- **The corpus and `baseline.tsv` are untouched**, as instructed.
- **Not every individual divergence was manually inspected** — minimisation
  (`bin/minimize-clusters.js`) re-verifies each cluster's *sample* still reproduces
  its exact signature and shrinks it, but does not re-inspect the other 4,839
  divergences a cluster's `count` summarises, beyond the T2 "Rust too permissive" case
  singled out above. A cluster count is not a severity ranking by itself (a common,
  low-severity message-text mismatch can outnumber a rare correctness gap), which is
  why T2's sub-patterns are called out by kind, not just by count.
- **This triage was not re-run against current `claude/tender-pascal-ocwf9q`.** #144
  (P2-08c) merged about 90 seconds before the previous revision of this file was
  posted, so T2's 311 clusters may already be partly fixed — see T2's owner section.
  Re-running `bin/fuzz.js` against a fresh WASM build is the way to find out, not
  assumed here (out of stage 1's scope; stage 2 is where the full re-run happens
  anyway).
- **Stage 2 (the 1,000,000-case run) has not been attempted.** At the throughput
  measured in this run (see `results/run-42.json`'s `started`/`finished`), it is a
  multi-hour single-core job; per the coordinator, it belongs after #144/#67 (and
  #156's T1 decision) land, sharded or parallelised.
