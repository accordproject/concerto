{
  "name": "concerto-e2e",
  "version": "5.0.0-beta.2",
  "private": true,
  "description": "End-to-end browser tests for Concerto web bundles.",
  "scripts": {
    "pretest:e2e": "npm --prefix .. run build",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
