name: Concerto Conformance Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  conformance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Target Project
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      - name: Install and Build Target Project
        run: |
          npm install
          npm run build

      - name: Checkout Conformance Suite
        uses: actions/checkout@v4
        with:
          repository: accordproject/concerto-conformance
          path: concerto-conformance

      - name: Install Conformance Suite Dependencies
        working-directory: concerto-conformance
        run: npm install

      - name: Run Conformance Tests
        working-directory: concerto-conformance
        env:
          PARSER_PATH: ../../../../../../packages/concerto-cto/dist/index.js
          MODELFILE_PATH: ../../../../../../packages/concerto-core/dist/index.js
          MODELMANAGER_PATH: ../../../../../../packages/concerto-core/dist/index.js
        run: npm test
        